// app/test-connection/layout.tsx
import React from 'react';

const TestConnectionLayout = ({ children }: { children: React.ReactNode }) => {
  return (
    <div>
      {/* You can add header, footer, or any common layout components here */}
      {children}
    </div>
  );
};

export default TestConnectionLayout;
